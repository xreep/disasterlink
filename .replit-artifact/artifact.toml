kind = "web"
previewPath = "/"
title = "Disaster Resource Coordination Platform"
version = "1.0.0"
id = "artifacts/disaster-platform"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 26017

[services.development]
run = "pnpm --filter @workspace/disaster-platform run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/disaster-platform", "run", "build" ]
publicDir = "artifacts/disaster-platform/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "26017"
BASE_PATH = "/"
